
<view class="gallery">

  <view class="item" wx:for="{{pictruesArray}}" wx:key="">
    <image src="{{item}}" data-src="{{item}}" bindtap="previewImage" mode="aspectFill" />
    <!-- 删除按钮 -->
    <view class="delete" bindtap="deleteImage" data-index="{{index}}">X</view>
  </view>

</view>